# Railway Configuration
# Ces variables seront automatiquement configurées par Railway

# Database (Railway PostgreSQL Plugin - Auto-configuré)
DATABASE_URL=postgresql://user:password@host:port/database

# Application
PORT=8080
SPRING_PROFILES_ACTIVE=prod

# JWT Security (À générer - voir guide ci-dessous)
JWT_SECRET=VotreSecretJWTTresSecuriseDauMoins256BitsLongPourHS256
JWT_EXPIRATION_MS=86400000
JWT_REFRESH_EXPIRATION_MS=604800000

# CORS (Votre URL GitHub Pages + Railway)
CORS_ALLOWED_ORIGINS=https://naciro2010.github.io,https://votre-app.railway.app

# Comment générer un JWT_SECRET sécurisé:
# openssl rand -base64 64
# ou
# node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
